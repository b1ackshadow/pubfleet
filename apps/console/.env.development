# Empty base URL makes the client issue relative /api requests, which the Vite dev
# server proxies to http://localhost:8081. That avoids any CORS configuration.
# Set an absolute URL here to talk to a control plane on another host.
VITE_API_BASE_URL=
